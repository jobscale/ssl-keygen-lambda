#!/bin/bash

. scripts/config

main() {
  echo '###################################'
  hostname
  id
  which openssl
  which bash
  which ssh
  which git
  echo '###################################'
  ls /bin | xargs
  echo '###################################'
  rm -fr $directory
  mkdir -p $directory
  scripts/ssl-keygen-server.sh
  scripts/ssl-keygen-client.sh
  ls -l $directory
}
main 2>&1
